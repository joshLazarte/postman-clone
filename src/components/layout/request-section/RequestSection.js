import React from 'react';
import RequestMenu from '../../menu/request-menu/RequestMenu';

const RequestSection = () => {
  return (
    <div className='RequestSection'>
      <RequestMenu />
    </div>
  );
};

export default RequestSection;
