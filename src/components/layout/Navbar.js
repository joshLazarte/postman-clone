import React from 'react';

const Navbar = () => {

    return (
        <nav className="text-left bg-primary py">
            <div className="container">
                <h4 className="text-left">Postman Clone</h4>
            </div>
        </nav>
    );
};

export default Navbar;
